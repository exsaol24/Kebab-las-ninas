{% extends 'base.html.twig' %}

{% block title %}{{ editar ? 'Editar Estado de Pedido' : 'Nuevo Estado de Pedido' }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/admin-panel.css') }}">
{% endblock %}

{% block body %}
<body class="admin-panel">
<div class="admin-edit-bg">
    <h1 class="admin-panel-title">{{ editar ? 'Editar Estado de Pedido' : 'Nuevo Estado de Pedido' }}</h1>
    <div class="admin-edit-form">
        {{ form_start(form) }}
            {{ form_row(form.nombre) }}
            <button class="btn btn-primary" type="submit">Guardar</button>
        {{ form_end(form) }}
        <a href="{{ path('admin_panel') }}" class="btn btn-secondary" style="margin-top: 15px;">Volver al panel</a>
    </div>
</div>
</body>
{% endblock %}